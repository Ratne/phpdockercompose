version: '3'
services:
    php:
        build:
            context: ./docker
            dockerfile: php.${PHPBUILD}.Dockerfile
        ports:
            - '9000'    
        volumes:
            - './app:/var/www/html'
    nginx:
        image: nginx:stable-alpine3.17
        ports:
            - '80'
            - '443'
        links:
            - 'php'
        volumes:
            - './app:/var/www/html'
            - './nginx/nginx.conf:/etc/nginx/'
            - './nginx/conf.d/default.conf:/etc/nginx/conf.d/'
